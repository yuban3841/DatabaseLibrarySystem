# ============================================================
# ClubCue 管理端 — 环境变量示例
# 复制为 .env 后按需修改；不存在的项取代码内的默认值
# ============================================================

# 后端 API 地址。
# 开发环境建议留空，直接走 vite.config.ts 里的 /api 反向代理（目标 3001）。
# 联调跨机部署时再显式指定，例如：
# VITE_API_BASE_URL=https://clubcue.example.com/api/v1

# 是否启用内置演示数据（mock）：
#   true  —— 不请求后端，全部走 src/api/mock.ts 的内存数据，可独立演示
#   false —— 走真实后端 /api/v1
# 后端契约（成员C/D）就绪后改成 false 即可，页面代码无需改动。
VITE_USE_MOCK=true

# ---------------- 内置演示账号（仅 mock 模式有效） ----------------
#   ADMIN（系统管理员）    admin / admin123
#   ORGANIZER（组织人）    org01 / org123
#   USER（社团成员）       u24050814 / user123
